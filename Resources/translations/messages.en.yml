batch_jobs:
    csv_product_import_archiver:
        label: Product Import Archiver
        validation.label:  File encoding validation

archiver:
    form:
        job_instance:
            tab:
                properties:
                    archiverEnabled:
                        title: Enable File Archiver
                        help: Decide if you want to stock the file in a specific directory
                    dir_archive:
                        title: Archive directory (if archiving enabled)
                        help: Path to the archive directory