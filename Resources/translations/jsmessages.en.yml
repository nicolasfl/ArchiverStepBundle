batch_jobs:
    csv_product_import_archiver:
        label: CSV Product Import with Archiver Step
    default_steps:
        archiver: Archiving
        import: Import

archiver:
    form:
        job_instance:
            tab:
                properties:
                    dir_archive:
                        title: Archives directory path
                        help: For instance ./var/archives/
                    archiverEnabled:
                        title: Enable File Archiver
                        help: Decide if you want to stock the file in a specific directory
